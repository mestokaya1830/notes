install docker and docker-compose
touch Dockerfile
touch .dockerignore
touch docker-compose.yaml
sudo docker compose up


//list images and containser-------------------------
sudo docker images //images list
sudo docker ps -a //container list


//delete images and stop containers-----------------
sudo docker container stop id or ctrl+c
sudo docker rmi id -f
