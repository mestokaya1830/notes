export default function Employees(name, lastName) {
  return name +' '+ lastName
}

export const Users = (firstname, lastName) => firstname +' '+ lastName
export const Admins = (firstname, lastName) => firstname +' '+ lastName